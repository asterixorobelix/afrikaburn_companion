# Backend environment variables — copy to .env and fill in values
# NEVER commit .env to source control

# JWT Configuration (all required — server will refuse to start if missing)
#
# JWT_SECRET: Random string, minimum 64 characters.
# Generate with: openssl rand -hex 32  (produces a 64-char hex string)
# The value below is intentionally too short and will fail startup validation.
JWT_SECRET=REPLACE_ME

# JWT_ISSUER: Identifies who issued the token (reverse domain convention)
JWT_ISSUER=io.asterixorobelix.afrikaburn

# JWT_AUDIENCE: Identifies the intended recipients of the token
JWT_AUDIENCE=io.asterixorobelix.afrikaburn-users
